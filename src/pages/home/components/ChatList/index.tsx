import React, { FC, useEffect, useState } from "react";
import { Select, message } from "antd";

import "./index.less";
interface ChatListProps {}

const ChatList: FC<ChatListProps> = (props) => {
  return <div className="chatListStyle"></div>;
};

export default ChatList;
